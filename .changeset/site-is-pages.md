---
"hm3": patch
---

**The site is now its homepage and its pages.** It moves to `@heroiclands/package-build@^22.4.0` and `@heroiclands/hugo-theme@^0.7.0`.

Removed from the site: section and tag index pages (`tags/**`), and the `homepage-root/` redirect. The package's page is published at `/hm3/` (the homepage).

The page count changes from 2 pages to 1 page: the single homepage at `/hm3/`.
